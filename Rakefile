require "bundler/gem_tasks"

require 'rubygems'
require "bundler"

Bundler.require
Mosaic.set_root "#{File.dirname(__FILE__)}/lib/blank_app"

require "#{Mosaic.root}/config/mosaic"
require 'mosaic/rake'